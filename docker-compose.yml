services:
  dashboard:
    build: .
    container_name: botcoin-dashboard
    restart: unless-stopped
    ports:
      - "80:8080"
    logging:
      driver: "json-file"
      options:
        max-size: "5m"
        max-file: "3"
